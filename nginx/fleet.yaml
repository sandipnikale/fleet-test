defaultNamespace: "" # If you're testing on a single cluster, you can simplify:
targetCustomizations:
 defaultNamespace: ""
 - name: test
   namespaceSelector:
     matchLabels:
       field.cattle.io/projectId: "p-tzd8d"
